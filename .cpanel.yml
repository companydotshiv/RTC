---
deployment:
  tasks:
    - export DEPLOYPATH=/home/icanalog/ranchiwebsite.com/projects/rtc
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -rf ts-app/dist/* $DEPLOYPATH/
    - /bin/cp -f ts-app/dist/.htaccess $DEPLOYPATH/.htaccess
